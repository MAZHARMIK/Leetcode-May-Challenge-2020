class Solution {
public:
    bool isPerfectSquare(int num) {
        unsigned int i;
        if(num==1 || num==0)
            return true;
        for(i=2;i<=num/2;i++) {
            if(i*i == num)
                return true;
        }
        return false;
    }
};
